<div>
	<div class="class_portrait">{{class_portrait}}</div>
	<div class="class_info info-box">
		<div class="main_info">
			<h4>{{writeString=class_detail.heading.teachers}}</h4>
			<p>{{class_teacher}}</p>
			<p class="count_students_info">{{count_students}} {{writeString=class_detail.students}}</p>
		</div>
		<div>{{schedule}}<br />
			{{schedule_extra}}</div>
	</div>
</div>
<span>{{class_portrait_label}}</span>
	{{news}}
<div class="documents">
	<h2>{{documents_title}}</h2>
	{{documents}}
</div>
<div class="links">
	<h2>{{links_title}}</h2>
	{{links}}
</div>
